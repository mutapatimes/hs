pandas>=2.0
openpyxl>=3.1
pytest>=8.0
requests>=2.33.0  # Shopify Admin API client (scoring/shopify_fetch.py); floor patches known CVEs
fastapi>=0.110    # halia.api — the query-interface HTTP service
python-multipart>=0.0.27  # parse the self-service /connect onboarding form; floor patches multipart DoS CVEs
uvicorn>=0.29     # ASGI server to run halia.api.app
httpx>=0.27       # FastAPI TestClient + sink HTTP calls
psycopg[binary,pool]>=3.1  # encrypted-secrets store + pool (Render); SQLite used locally
PyJWT>=2.8        # verify Shopify App Bridge session tokens (embedded app auth)
cryptography>=42  # encrypt the only persisted secrets (Shopify token, Klaviyo key) at rest
pywebpush>=1.14   # Web Push desktop alerts for high-grade orders (optional; no-op if no VAPID)
weasyprint>=62    # catalog-PDF generator (HTML->PDF). Needs cairo/pango system libs — see Dockerfile.
anthropic>=0.69   # "Draft with Halia" / the conversation brief (halia/llm.py). Idle without a key.
segno>=1.6
sentry-sdk>=2.0   # error tracking; idle without SENTRY_DSN
# Security floors for real transitive dependencies (surfaced by pip-audit in CI). Direct pins so
# a fresh install pulls the patched version regardless of what the parent package resolves. These
# fixed versions need Python >=3.10 — production and CI both run 3.11 (see Dockerfile).
urllib3>=2.7.0    # via requests; HTTP CVE fixes
click>=8.3.3      # via uvicorn; CVE fix
# NOTE: starlette (via fastapi) also has an advisory whose fix (1.1.0) is a major bump coupled to
# a newer FastAPI; deferred to a coordinated FastAPI upgrade rather than pinned blindly here.

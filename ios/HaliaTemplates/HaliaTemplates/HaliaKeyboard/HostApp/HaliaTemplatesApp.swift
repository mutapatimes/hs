// Target membership: HOST APP ONLY. The app entry point.
import SwiftUI

@main
struct HaliaTemplatesApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
